<% content_for :title, "Add a New Movie" %>

<div class="header-container">
  <h1>Add Another Movie</h1>

  <%= link_to "Home", movies_path(sort: @sort, direction: @direction), class: "btn btn-secondary" %>
</div>

<%= render "form", movie: @movie %>

<br>